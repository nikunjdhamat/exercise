import React from 'react';
import MovieListContainer from './containers/movieList.container';

const HomeScreen: React.FunctionComponent = () => {
  return <MovieListContainer />;
};

export default HomeScreen;
